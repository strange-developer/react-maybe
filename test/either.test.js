import { expect } from 'chai';

describe('Either', () => {
  describe('constructor', () => {
    it('creates wrapped instance', () => {
      const isTrue = true;
      expect(isTrue).to.be.true;
    });
  });
});
